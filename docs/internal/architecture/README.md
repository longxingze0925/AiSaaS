# 架构文档

系统架构、数据库、权限错误码和完整内部接口资料放在这里。

## 文件

- `SaaS_AI计费改造方案.md`：SaaS + AI 计费模式的核心架构方案。
- `数据库表结构.md`：数据库表、索引、迁移和字段含义。
- `权限点与错误码清单.md`：RBAC 权限点和稳定错误码。
- `API接口文档.md`：完整内部 API 字典，包含后台、Server API、兼容区等全部接口。

给三方业务平台接入时，不直接发送本目录；对外入口使用 `../../public/三方接口接入文档.md`。
