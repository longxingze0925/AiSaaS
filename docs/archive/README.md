# 历史兼容资料

`docs/archive/` 放非主线或历史兼容资料，不作为新功能开发入口。

## 文件

- `客户端SDK设计.md`：旧客户端授权、设备绑定、签名、更新包校验等兼容能力设计。
- `Web 后端接入指南-原始版.md`：旧版 Web 后端接入说明，已由 `../public/三方接口接入文档.md` 取代。

新三方接入以 `../public/三方接口接入文档.md` 为准。
