# Migrations

SQL migrations for this backend live in this directory.

Create migration files with `sqlx migrate add <name>` and keep schema changes synchronized with `../../docs/internal/architecture/数据库表结构.md`.
